# script.report — Python dependencies (flat fallback)
# Authoritative dep list lives in pyproject.toml. This file mirrors it for
# anyone running:  pip install -r requirements.txt --break-system-packages
# Editable install preferred:  pip install -e .

requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
pdfplumber>=0.10.0
anthropic>=0.25.0
python-dotenv>=1.0.0
openpyxl>=3.1.0
voyageai>=0.3.0
numpy>=1.24
